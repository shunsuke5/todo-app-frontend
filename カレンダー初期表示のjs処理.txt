カレンダー初期表示のjs処理

なんかうまくいっていない。
　→　関数呼び出しのかっこを付け忘れていた。前々からやろうと思っていたvscodeの設定変更をした。

setDate()で曜日が変わっていない。
　→　変わってた。勘違い。

今年、今月を取得する
そのカレンダーを初期表示する

1週目から順に格納していく
月のループ内で週のループを行う？
月のループはtbodyのforEach,週のループはtbody.tdのforEach()
その月の最初の日付を取得し、その曜日とインデックスが重なればそこに配置できる。
あとは順にセットしていくだけ。